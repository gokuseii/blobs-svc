log:
  disable_sentry: true

db:
  url: "postgres://blobs:blobs@localhost/blobs?sslmode=disable"

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: blobs
  service_port: 80